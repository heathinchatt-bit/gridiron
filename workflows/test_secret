name: Test Secret

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check Secret
        run: |
          if [ -z "${{ secrets.TEST_SECRET }}" ]; then
            echo "Secret is empty or not set"
            exit 1
          else
            echo "Secret exists and has a value"
          fi
